<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">
    <insert id="inserBatch">
        insert into order_detail (
        name, image, order_id, dish_id, setmeal_id, dish_flavor, number, amount
        ) value
        <foreach collection="orderDetailList" item="item" separator=",">
            (
            #{item.name},
            #{item.image},
            #{item.orderId},
            #{item.dishId},
            #{item.setmealId},
            #{item.dishFlavor},
            #{item.number},
            #{item.amount}
            )
        </foreach>
    </insert>
    <select id="getByOrderId" resultType="com.sky.entity.OrderDetail">
        select * from order_detail
        <where>
            order_id = #{orderId}
        </where>
    </select>

    <select id="countByMap" resultType="com.sky.dto.GoodsSalesDTO">
        select
            od.name,
            sum(od.number) AS number
        from order_detail od
            left join orders o
                on od.order_id = o.id
        <where>
            <if test="status != null">
                and o.status = #{status}
            </if>

            <if test="begin != null">
                and order_time &gt; #{begin}
            </if>

            <if test="end != null">
                and order_time &lt; #{end}
            </if>
        </where>
        GROUP BY od.name
        order by number desc
        limit 10;
    </select>
</mapper>

